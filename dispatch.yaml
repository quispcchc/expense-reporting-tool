dispatch:
  # Route all API and storage requests to the 'api' service (backend)
  - url: "*/api/*"
    service: api
  - url: "*/storage/*"
    service: api

  # Route everything else to the 'default' service (frontend)
  - url: "*/*"
    service: default
